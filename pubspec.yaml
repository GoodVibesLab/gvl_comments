name: gvl_comments
description: Drop-in comments system for Flutter apps. Powered by GoodVibesLab Cloud.
version: 0.1.0
homepage: https://goodvibeslab.cloud

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  package_info_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true